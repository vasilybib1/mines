services:
  frontend:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "8080:8080"

  backend:
    build: ./backend
    restart: unless-stopped
    ports:
      - "3000:8080"
        
