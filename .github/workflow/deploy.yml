name: auto deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: checkout repo code
        uses: actions/checkout@v4

      - name: build and run app
        run: | 
          echo "deploying"
